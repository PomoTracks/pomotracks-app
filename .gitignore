# Ignore local Terraform directories and files
# These are generated by 'terraform init' and can be downloaded again.
**/.terraform/
**/.terraform.lock.hcl

# Ignore local state files
# These can contain sensitive information in plain text.
*.tfstate
*.tfstate.*

# Ignore crash log files
crash.log
crash.*.log

# Ignore variable files that may contain secrets.
# It's a best practice to never commit variable files with secrets.
*.tfvars
*.tfvars.json

# Ignore plan output files
# These can also contain sensitive data.
*.tfplan

# Ignore SSH keys and other sensitive file types
*.pem
*.key
# You could even add *.pub here if you are extra cautious,
# but it's not strictly necessary.

# Ignore OS-specific files
.DS_Store
Thumbs.db